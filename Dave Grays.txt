add event listener
(target, event, callback, capture = false)
target.addEventListener(event, callback, !!capture)

sanitizeInput (inputValue, dom = document)
const div = dom.createElement('div');
div.textContent = inputValue;
return div.innerHTML; // innerText;

_ce: consider methods in keyTypes.

delete child element (no-recursive) // ?
dce = (parent) => {
    let child = parentElement.lastElementChild;
    while (child) {
        parentElement.removeChild(child);
        child = parentElement.lastElementChild;
    }
}

addCssClass (selector, className, scope)

